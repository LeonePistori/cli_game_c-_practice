text_file.open("resources/" + player.position + ".txt", std::ios::in);
if (!text_file.is_open()) {
	std::cout << "Level file not found, press enter to close the game\n";
	std::cin.get();
	break;
}

level_text.clear();

while (std::getline(text_file, next_line)) {
	level_text = level_text + next_line;
}
std::cout << level_text << '\n';